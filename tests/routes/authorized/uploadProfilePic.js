import r from 'rethinkdb'
import { expect } from 'chai'
import ajax from '../../../src/helpers/ajax'

export default ({ domain, user }) => {
  let url = `${domain}/api/uploadProfilePic`

  describe(`uploadProfilePic`, () => {
    it(`endpoint should exist`)
  })
}
