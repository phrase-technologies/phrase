/*
import React from 'react'
import { shallow } from 'enzyme'
import { expect } from 'chai'
import { App } from '../App'
*/

/*
 *  Shallow
 */

/*    // TODO: Commented out until we can figure out how to
      // replicate the webpack aliases (e.g. 'component') here
describe('<App />', () => {
  it('should exist', () => {
    let component = shallow(<App dispatch={ () => {} } />)
    expect(component).to.exist
  })
})
*/
